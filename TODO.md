- [x] Inspect repo for existing `api/index.ts` entrypoint (or confirm it’s missing)
- [ ] Update `vercel.json` to use the correct API entrypoint (current repo deploys `server.ts`)
- [ ] Validate build locally (e.g., `npm run build` / `vercel build` if available)
- [ ] Deploy to Vercel and verify `/api/...` routes


